# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build output (regenerated by `cargo build` / `tauri dev`)
src-tauri/target/
src-tauri/gen/
**/*.rs.bk

# Local AI model weights — Trackwise talks to Ollama over HTTP, but ignore these
# in case any model files ever get downloaded/cached locally during experimentation
*.gguf
*.ggml
*.safetensors
*.onnx
models/
